name: DG Monitor (every 5 minutes)

on:
  schedule:
    - cron: '*/5 * * * *'   # 每 5 分钟执行一次
  workflow_dispatch:        # 可以手动触发

permissions:
  contents: write   # 允许写回 repo（用于更新 state.json）

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # install playwright browsers
          python -m playwright install --with-deps

      - name: Run DG detector
        env:
          TELEGRAM_TOKEN: 8134230045:AAH6C_H53R_J2RH98fGTqZFHsjkKALhsTh8
          TELEGRAM_CHAT_ID: 485427847
          DG_URL_1: https://dg18.co/wap/
          DG_URL_2: https://dg18.co/
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TZ: "Asia/Kuala_Lumpur"
        run: |
          python detector.py
